"""CricFloat — always-on-top live India cricket score overlay for macOS."""

# Keep in sync with setup.py's CFBundleShortVersionString. Used as the fallback
# version when running as a plain script (outside the .app bundle).
__version__ = "1.1.0"
